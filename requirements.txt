# Core dependencies for the Financial Sentiment & Market Signal project.
# CPU-only. Versions pinned from a verified working install (Python 3.11).
#
# torch is the CPU build. Install it from the PyTorch CPU wheel index so pip does not
# pull CUDA wheels:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# then install the rest normally:
#   pip install -r requirements.txt

transformers==5.13.1
accelerate==1.14.0
datasets==5.0.0
torch==2.13.0
scikit-learn==1.9.0
xgboost==3.2.0
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
streamlit==1.59.2

# Dev / tooling
pytest==9.1.1
huggingface_hub
